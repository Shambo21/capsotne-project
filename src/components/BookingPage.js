import BookingForm from "./BookingForm";
const Reservations = () => {

    return (
        <div className="hero">
            <div className="container">
                <BookingForm />
            </div>
        </div>
    )
}

export default Reservations;